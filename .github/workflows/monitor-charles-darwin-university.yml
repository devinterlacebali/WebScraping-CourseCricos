name: "Monitor: Charles Darwin University"

on:
  workflow_dispatch:

concurrency:
  group: monitor-charles-darwin-university
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Run monitor
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python monitor_changes.py --name "Charles Darwin University" --scraper "Charles Darwin University/cdu.py" --sql "Charles Darwin University/cdu_update.sql" --snapshot "Charles Darwin University/snapshot.json"

      - name: Commit snapshot & changes SQL
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "Charles Darwin University/snapshot.json"
          git add "Charles Darwin University"/*_changes_*.sql || true
          if git diff --cached --quiet; then
            echo "Nothing to commit."
          else
            git commit -m "chore: snapshot & changes for Charles Darwin University [skip ci]"
            git push
          fi
