name: "Monitor: Western Sydney University"

on:
  workflow_dispatch:

concurrency:
  group: monitor-western-sydney-university
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Run monitor
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python monitor_changes.py --name "Western Sydney University" --scraper "Western Sydney University/western.py" --sql "Western Sydney University/wsu_update_courses.sql" --snapshot "Western Sydney University/snapshot.json"

      - name: Commit snapshot & changes SQL
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "Western Sydney University/snapshot.json"
          git add "Western Sydney University"/*_changes_*.sql || true
          if git diff --cached --quiet; then
            echo "Nothing to commit."
          else
            git commit -m "chore: snapshot & changes for Western Sydney University [skip ci]"
            git push
          fi
